#include<stdio.h>
#include<string.h>
struct student
{
char name[10];
int rollno;
int phno;
};
void main()
{
struct student *cptr,c1;
cptr = &c1;
printf("Enter the details of student");
scanf("%s%d%d",cptr->name,&cptr->rollno,&cptr->phno);
printf("\n%s  %d  %d",(*cptr).name,(*cptr).rollno,(*cptr).phno);
}
