#include<stdio.h>
int linearsearch(int a[],int n);
void main()
{
int n,a[10];
printf("enter n elements");
scanf("%d",&n);
linearsearch(a,n);
}
int linearsearch(int a[],int n)
{
int k;
if (n==0)
return -1;
else
{
 if(k==a[n-1])
return n-1;
else 
return linearsearch(a,n-1,k);
}
}
