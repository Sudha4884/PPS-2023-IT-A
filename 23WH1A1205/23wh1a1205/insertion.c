#include<stdio.h>
void insertion(int a[],int n);
void main()
{
 int i,a,j,size;
 printf("enter the size of array");
 scanf("%d",&size);
 for(i=0;i<n;i++)
{
  j=i-1;
 while  ((j>=0) && (a(j)>a(i))

  a[j+1]=a[j];
  j--;

a(j+1)=a[i]
}
}
