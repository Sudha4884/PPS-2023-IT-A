#include<stdio.h>
#include<math.h>
void main()
{
float  p , t, r , SI,CI;
printf("Enter p,t,r values:");
scanf("%f%f%f",&p, &t ,&r);
SI = p*t*r/100;
CI = p*(pow((1 + r /100),t));
printf("\n simple interest is %f",SI);
printf("\n compound interset is %f",CI);
}
