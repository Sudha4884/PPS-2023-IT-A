#include <stdio.h>
// Area of triangle
int main() {
    int l,b,h,area;
    printf("enter values of l,b,h");
    scanf("%d%d%d",&l,&b,&h);
    area = l*b*h;
    printf("area of triangle is = %d",area);
}
