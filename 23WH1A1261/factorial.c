 // A  program to display the factiorial of a given integer//
#include<stdio.h>
int main()
{
int i,n;
 int factorial=1;
printf("entre a number to find factorial");
scanf("%d",&n);
for(i=1;i<=n;i++);
{
factorial=factorial*
}
printf("factorial of %d is %lld",n,fact);
return 0;

} 
