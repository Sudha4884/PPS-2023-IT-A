#include <stdio.h>

void main() {
int p;
int r ;
int t;
int n;
    printf("give principal amount,rate of interest,time period");
    scanf("%d%d%d",&p,&r,&t);
    int si=(p*t*r)/100;
    int ci=p*pow((1+r/100),t);
    printf("%d",si);
    printf("\n%d",ci);
}
