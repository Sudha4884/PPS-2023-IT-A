#include <stdio.h>

int main() {
    int i, num, c=0;
    printf("enter a number :");
    scanf("%d",&num);
    for(i=2;i<num;i++)
    {
        if((num%i)==0)
        c++;
    }
    if(c==0)
    printf("\n %d is prime ",num);
    else
    printf("\n %d not is prime ",num);
    return 0;
}
