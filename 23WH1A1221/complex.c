#include<stdio.h>
struct complex
{
int real;
float imaginary;
};
struct complex com;
void main()
{
printf("\n Enter real, imaginary");
scanf("%s%d%d", com[i].real, &com[i].imaginary);
printf("%s%d+%di", com[i].real, com[i].imaginary);
}
