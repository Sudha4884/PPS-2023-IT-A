#include <stdio.h>
void main()
{
    int marks,m;
    printf("Give your marks out of 100");
    scanf("%d",&m);
    if(m>70)
    {
    printf("Distinction");
    }
    else if(m>60 && m<70)
    {printf("First class");
    }
    else if(m>40 && m<60)
    {
    printf("Second class");
    }
    else
    {printf("failed");
    }
    }
