#include <stdio.h>
#include <math.h>
void main()
{
    int a,n;
    float x,sum=0;
    printf("Input the Value of x :");
    scanf("%f", &x);
    for(a=0;a<n;a++)
    {
       n = pow(x, exp);
       sum = sum + n;
    }
    printf("Value = %f",sum);
}
