//learning bubble sort
#include<stdio.h>
int swap();
int main()
{
	int a[i];
	printf("enter number of elements in list:");
	scanf("%d",&a[i]);
	printf("sorted list is %d ",a[i]);
	return a[i];
}
int swap()
	int i,j,n;
	for(i=0;i<n;i++)
	{
		for(j=0;j<n-i-1;j++)
		{
			if(a[j] > a[j+1])
			{
			swap(a[j],a[j+1]);
			}
		}
	}

