





















































































































































































































































































































































































































































































































































































































































































































































































































































































#include<stdio.h>
void is_prime(int);
void main()
{
int starting,ending;
printf("enter the starting:");
scanf("%d",&starting);
printf("enter the ending:");
scanf("%d",&ending);
for (int num=starting;num<=ending;num++)
is_prime(num);
}
void is_prime(int n)
{
int i,count=0;
for (i=1;i<=n;i++)
{
if (n % i == 0)
count++;
}
if (count==2)
printf("%d",n);
}



																																																																																																																																																																																																																																																									

